[build]
buildCommand = "npm install && npx prisma generate --schema=apps/api/prisma/schema.prisma"

[deploy]
startCommand = "npx tsx apps/api/src/server.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
